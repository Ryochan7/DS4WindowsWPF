<UserControl x:Class="DS4WinWPF.DS4Forms.RecordBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DS4WinWPF.DS4Forms"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="800" KeyDown="UserControl_KeyDown" KeyUp="UserControl_KeyUp">
    <DockPanel Margin="4">
        <DockPanel DockPanel.Dock="Right" Width="200" LastChildFill="False" Margin="4,0">
            <StackPanel DockPanel.Dock="Top">
                <UniformGrid Rows="1">
                    <Button x:Name="saveBtn" Content="Save" Click="SaveBtn_Click" Margin="0,0,5,0" />
                    <Button x:Name="cancelBtn" Content="Cancel" Click="CancelBtn_Click" Margin="5,0,0,0" />
                </UniformGrid>

                <Button x:Name="recordBtn" Content="Record" Margin="0,10,0,0" Click="RecordBtn_Click" />

                <UniformGrid Rows="1" Margin="0,10,0,0">
                    <Button x:Name="loadPresetBtn" Content="Load Preset" Margin="0,0,5,0" Click="LoadPresetBtn_Click">
                        <Button.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Cycle Programs" />
                                <MenuItem Header="From File..." />
                            </ContextMenu>
                        </Button.ContextMenu>
                    </Button>
                    <Button x:Name="savePresetBtn" Content="Save Preset" Margin="5,0,0,0" Click="SavePresetBtn_Click" />
                </UniformGrid>

                <CheckBox x:Name="recordDelaysCk" Content="Record Delays" HorizontalAlignment="Center" Margin="0,10,0,0" />
                <TextBlock Text="Double click on a wait to edit the time" Margin="0,10,0,0" />
                <ComboBox x:Name="macroModeCombo" SelectedIndex="0" Margin="0,10,0,0">
                    <ComboBoxItem Content="Play Once" />
                    <ComboBoxItem Content="Repeat While Held" />
                </ComboBox>
            </StackPanel>
            <StackPanel DockPanel.Dock="Bottom">
                <StackPanel x:Name="mouseButtonsPanel">
                    <Button x:Name="fourMouseBtn" Content="4th Mouse Button" Width="120" />
                    <Button x:Name="fiveMouseBtn" Content="5th Mouse Button" Width="120" Margin="0,10,0,0" />
                </StackPanel>

                <StackPanel x:Name="extraConPanel">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
                        <Button x:Name="addRumbleBtn" Content="Add Rumble" Width="124" Click="AddRumbleBtn_Click" />
                        <Image Source="/DS4WinWPF;component/Resources/left touch.png" Width="43" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                        <Button x:Name="changeLightBtn" Content="Change Lightbar Color" Width="124" Click="ChangeLightBtn_Click" />
                        <Image Source="/DS4WinWPF;component/Resources/left touch.png" Width="43" />
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </DockPanel>
        <DockPanel DockPanel.Dock="Left">
            <DockPanel DockPanel.Dock="Top">
                <Label Content="Macro Order" />
                <Label Content="Use Keyboard/Mouse + Controller 1 to record" DockPanel.Dock="Right" HorizontalAlignment="Right" />
            </DockPanel>
            <ListBox x:Name="macroListBox" SelectedIndex="{Binding MacroModeIndex}">
                
            </ListBox>
        </DockPanel>
    </DockPanel>
</UserControl>
